# 2026-07-09

- 修复 06B 背景图影响其他栏 + iPad消失/滚动条失效问题：`::before` 最终使用 `position: absolute; inset: 0; background-attachment: fixed` — 不占布局空间，背景固定在视口但仅限06B区块内可见。
- 06B 滚动条调整：iPad 右侧 18px，高 540px；轨道透明白 rgba(255,255,255,0.25) 圆角 18px；手柄 #655D4E 圆角 5px；恢复双向同步（拖拽/点击 ↔ 页面滚动）。
- 修复 `work-nenovia-anchor.html` 锚点导航：dot 匹配从数组索引改为 `data-target === section.id`，使用 `getBoundingClientRect()` 替代 `offsetTop`，避免 `#comparison` 无对应 dot 越界报错。
- 更新 `work-nenovia-anchor-backup.html` 画廊图片顺序：03配色 顺序改为 Group 205 → Group 306 → Group 202 → Group 203 → Group 204；04 VI设计 顺序改为 111 → 222 → 333 → 444 → Group 300 → Group 296 → Group 305。
- 更新 `work-nenovia-anchor-backup.html` 07 总结区块背景为 `img/xin/图片节点 7 1.png`（center/cover，保留 `var(--bg)` 作为 fallback）。
- 07 总结表格色块改为 rgba 半透明：外层 75% 白 + 毛玻璃模糊；表头 82% 透明度；表体 7 行 70% 透明度；总结条 85% 透明度。
- 07 总结标题 `Before & After` 改为 `升级前后总结`，白色，与表格间距 40px。
- 01 项目概览标题「项目概览·三阶段结构」改为白色字体。
- 02 问题分析区块背景先后更新为 `img/xin/豆包---2026-07-09T175614.png`，最终改为 `img/xin/22.png`（center/cover）。
- 02 区块多项调整：新增标题「核心问题诊断」白色 Playfair Display；表格色块 rgba 半透明（外层 75% 白+毛玻璃、表头 82%、表体 70%）；品牌全链条文案改为白色+深棕半透明色块居中；表格上边距推移至 240px。
- 01/02 背景均通过 `::before` 实现模糊 `blur(6px)` + 20% 黑色遮罩，父元素 `background: transparent`。
- 02 表格内边距从 240px 恢复为 60px 居中；2/3 列文字颜色从 `var(--text-sec)` 50% 透明改为实色 `#1a1a1a`；行 hover 效果 `rgba(255,255,255,0.85)`；品牌全链条色块圆角改为 24px 与表格一致 + 24px 间距。
- 锚点导航 `nav-light` 色彩从黑色系改为白色半透明 `rgba(255,255,255,0.5)`，适配深色底图；导航项间距从 30px 缩小到 18px。
- 标题字号统一：01/02/03/04 标题均为 `clamp(24px,3vw,40px)` + Playfair Display；07 总结标题 `clamp(28px,4vw,48px)`。
- 07 总结区块背景改为 `::before` + `blur(4px)` + 25% 黑色遮罩，父元素 `background: transparent`。
- 全部画廊（03/04/05b）改为连续平滑自动滚动：克隆图片实现无缝循环，`requestAnimationFrame` 每帧 0.4px，交互后暂停 2s 恢复。
- 画廊自动滚动改为 ping-pong 往返模式（不克隆图片，到末尾反向回弹）：速度 0.8px/帧，threshold 0.05，初始化时检查视口可见性直接启动；用户点击箭头/拖拽后永久停止自动滚动。
- 修复锚点点击跳动：`html{scroll-behavior:smooth}` → `auto`；用自定义 `window.scrollTo` + RAF 替换 `scrollIntoView`；预触发目标区块 `.reveal` 动画（`transition:none` + reflow）；IntersectionObserver 动画期间跳过更新；`locked` 延迟 350ms 解锁；点击时立即设置 `active` 状态 + `.animating` 类禁用所有 transition。
- 全项目图片批量 WebP 转换：扫描 24 个 HTML 文件提取 85 个图片引用，Pillow quality=85 method=6 转换 33 张新图片 + 9 张已有 WebP，6 个 HTML 文件更新引用。总计 69.5MB → 6.9MB（90% 压缩）。`convert_webp.py` 脚本保留可复用。
